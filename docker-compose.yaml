version: "3"

services: 
  valheim: 
    image: lloesche/valheim-server
    volumes: 
      - $HOME/valheim/settings:/config
      - $HOME/valheim/data:/opt/valheim
    ports: 
      - "2456-2458:2456-2458/udp" 
    env_file:
      - $HOME/valheim/valheim.env
    restart: always
